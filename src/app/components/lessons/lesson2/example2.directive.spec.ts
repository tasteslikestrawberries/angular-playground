import { Example2Directive } from './example2.directive';

describe('Example2Directive', () => {
  it('should create an instance', () => {
    const directive = new Example2Directive();
    expect(directive).toBeTruthy();
  });
});
